---
sidebar_label: 2015年2月実施 問題1
tags:
  - Tokyo-University
  - Mathematics.Linear-Algebra.Eigenvalues-and-Eigenvectors
  - Mathematics.Linear-Algebra.Inner-Product-and-Orthogonality
  - Mathematics.Linear-Algebra.Matrix-Determinant
---
# 東京大学 情報理工学系研究科 コンピュータ科学専攻 2015年2月実施 問題1

## **Author**
[kainoj](https://github.com/kainoj/utokyo-cs), 祭音Myyura

## **Description**
(1) Suppose that $A$ is a real symmetric matrix. Let $\boldsymbol{x}$ and $\lambda$ be an eigenvector and the corresponding eigenvalue of $A$, respectively. Prove that $\lambda$ is a real number.

(2) Suppose that $A$ is a real symmetric matrix. Let $\boldsymbol{x}_1$ and $\boldsymbol{x}_2$ be eigenvectors of $A$; and $\lambda_1$ and $\lambda_2$ be the eigenvalues of $A$ that correspond to $\boldsymbol{x}_1$ and $\boldsymbol{x}_2$, respectively. Furthermore, assume that $\lambda_1 \neq \lambda_2$.

Prove that $\boldsymbol{x}_1 \cdot \boldsymbol{x}_2 = 0$. Here $\boldsymbol{x}_1 \cdot \boldsymbol{x}_2$ denotes the inner product of the two vectors $\boldsymbol{x}_1$ and $\boldsymbol{x}_2$.

(3) Let $A$ be a matrix

$$
A = \begin{pmatrix}
x & a_1 & b_1 & c_1 \\
y & a_2 & b_2 & c_2 \\
z & a_3 & b_3 & c_3 \\
1 & 1 & 1 & 1
\end{pmatrix}
$$

where $a_i$, $b_i$, and $c_i$ ( $i \in \{1, 2, 3\}$) are real numbers such that the two vectors $(b_1 - a_1, b_2 - a_2, b_3 - a_3)$ and $(c_1 - a_1, c_2 - a_2, c_3 - a_3)$ are linearly independent. 
Let us define a function $f: \mathbb{R}^3 \to \mathbb{R}$ by  

$$
f(x, y, z) = \text{det}(A)
$$

Prove that the equation $f(x, y, z) = 0$ determines a plane in the $xyz$ space.

(4) In the setting of Question (3), give three points that lie on the plane determined by $f(x, y, z) = 0$.

### 题目描述

（1）设 $A$ 为实对称矩阵，$\boldsymbol{x}$ 是 $A$ 的特征向量，对应特征值为 $\lambda$。证明 $\lambda$ 是实数。

（2）设 $A$ 为实对称矩阵，$\boldsymbol{x}_1,\boldsymbol{x}_2$ 分别是对应特征值 $\lambda_1,\lambda_2$ 的特征向量，且 $\lambda_1\ne\lambda_2$。证明

$$
\boldsymbol{x}_1\cdot\boldsymbol{x}_2=0,
$$

其中“$\cdot$”表示向量内积。

（3）令

$$
A=\begin{pmatrix}
x&a_1&b_1&c_1\\
y&a_2&b_2&c_2\\
z&a_3&b_3&c_3\\
1&1&1&1
\end{pmatrix},
$$

其中 $a_i,b_i,c_i\ (i\in\{1,2,3\})$ 均为实数，并且向量
$(b_1-a_1,b_2-a_2,b_3-a_3)$ 与
$(c_1-a_1,c_2-a_2,c_3-a_3)$ 线性无关。定义
$f:\mathbb{R}^3\to\mathbb{R}$ 为

$$
f(x,y,z)=\det(A).
$$

证明方程 $f(x,y,z)=0$ 在 $xyz$ 空间中确定一个平面。

（4）在第（3）问的设定下，给出位于该平面上的三个点。

## **Kai**
### (1)
Let $A\in \mathbb{R}^{n\times n}$ such that $A = A^T$ and $Ax = \lambda x$ for some eigenvector $x$ and corresponding eigenvalue $\lambda$.
Suppose that $\lambda \in \mathbb{C}$, i.e. $\lambda = a + ib$ for some $a,b\in\mathbb{R}$.
Let's start with $Ax = \lambda x$:

$$
\begin{aligned}
    Ax &= \lambda x && \text{take conjugate transpose} \\
    x^H A^H &= \lambda^H x^H && \text{$A$ is real symmetric, so $A=A^H$} \\
    x^H A &= \lambda^H x^H && \text{multiply by  $x$} \\
    x^H A x &= \lambda^H x^H x && \text{now,  $Ax = \lambda x$} \\
    \lambda x^H x &= \lambda^H x^H x  \\
    \lambda &= \lambda^H \\
    a + bi &= a - bi
\end{aligned}
$$

Hence, $\text{Im}(\lambda)$ = 0, which means $\lambda \in \mathbb{R}$.

### (2)
Start with the first eigenpair:

$$
\begin{aligned}
    Ax_1 &= \lambda_1 x_1   && \text{take conjugate transpose; $A=A^H$, $\lambda_1\in\mathbb R$} \\
    x_1^H A &= \lambda_1 x_1^H && \text{multiply by $x_2$} \\
     x_1^H A x_2 &= \lambda_1 x_1^H x_2 &&  \\
\end{aligned}
$$

Now fiddle with the second eigenpair:

$$
\begin{aligned}
    Ax_2 &= \lambda_2 x_2   && \text{left-multiply by $x_1^H$} \\
    x_1^H Ax_2 &= \lambda_2 x_1^H x_2
\end{aligned}
$$

If we subtract both result, we get:

$$
\begin{aligned}
    0 = (\lambda_1 - \lambda_2) x_1^H x_2
\end{aligned}
$$

Since $\lambda_1 \neq \lambda_2$, then $x_1^H x_2 = 0$.
In other words, $x_1 \cdot x_2 =0$.

### (3)

Put $p=(x,y,z)^T$, $a=(a_1,a_2,a_3)^T$, and similarly define $b,c$.
Subtracting the second column from the other three gives

$$
f(x,y,z)=0
\iff \det\begin{pmatrix}p-a&b-a&c-a\end{pmatrix}=0.
$$

Since $b-a$ and $c-a$ are linearly independent, this is equivalent to

$$
p=a+s(b-a)+t(c-a) \qquad (s,t\in\mathbb R),
$$

which is a plane.

### (4)
The three points are

$$
(a_1,a_2,a_3),\qquad (b_1,b_2,b_3),\qquad (c_1,c_2,c_3).
$$

At each point, the first column of $A$ equals one of the other columns.
