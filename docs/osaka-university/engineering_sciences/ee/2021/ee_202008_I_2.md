---
sidebar_label: "2020年8月実施 電子光科学 [I-2]"
tags:
  - Osaka-University
  - Mathematics.Linear-Algebra.Linear-Operator-on-Polynomial-Space
  - Mathematics.Linear-Algebra.Eigenvalues-and-Eigenvectors
---
# 大阪大学 基礎工学研究科 電子光科学 (システム創成専攻) 2020年8月実施 電子光科学 \[I-2\]

## **Author**
[Miyake](https://miyake.github.io/exams/index.html)

## **Description**

### 题目描述

原文题干缺失。设 $V$ 为次数不超过 2 的实系数多项式空间，并定义线性变换
$$
(Tf)(x)=f(x+1).
$$

1. 给出 $V$ 的一组基 $u(x),v(x),w(x)$。
2. 求 $T$ 关于该基的表示矩阵。
3. 求表示矩阵的特征值与特征向量。

#### 考点

- **多项式向量空间**：使用 $\{x^2,x,1\}$ 作为标准基。
- **线性变换的表示矩阵**：把每个基向量的像在原基下展开并按列排列。
- **特征值与特征向量**：分析上、下三角表示矩阵的谱及特征空间。
- **平移算子**：理解变量替换 $x\mapsto x+1$ 对多项式系数的作用。

## **Kai**
### (1)

$$
  \begin{aligned}
  u(x) = x^2
  , \ \ 
  v(x) = x
  , \ \ 
  w(x) = 1
  \end{aligned}
$$

### (2)

$$
  \begin{aligned}
  T(u(x))
  &= T(x^2) = (x+1)^2 = x^2 + 2x + 1
  \\
  &= u(x) + 2v(x) + w(x)
  \\
  T(v(x))
  &= T(x) = x+1
  \\
  &= v(x) + w(x)
  \\
  T(w(x))
  &= T(1) = 1
  \\
  &= w(x)
  \end{aligned}
$$

であるから、

$$
  \begin{aligned}
  \begin{pmatrix} T(u(x)) & T(v(x)) & T(w(x)) \end{pmatrix}
  =
  \begin{pmatrix} u(x) & v(x) & w(x) \end{pmatrix}
  \begin{pmatrix} 1 & 0 & 0 \\ 2 & 1 & 0 \\ 1 & 1 & 1 \end{pmatrix}
  \end{aligned}
$$

であり、求める表現行列は、

$$
  \begin{aligned}
  A = \begin{pmatrix} 1 & 0 & 0 \\ 2 & 1 & 0 \\ 1 & 1 & 1 \end{pmatrix}
  \end{aligned}
$$

である。

### (3)
$A$ の固有値は $1$ であり、対応する固有ベクトルは、

$$
  \begin{aligned}
  \begin{pmatrix} 0 \\ 0 \\ 1 \end{pmatrix}
  \end{aligned}
$$

である。
