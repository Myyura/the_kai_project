---
sidebar_label: "2018年8月実施 H2 相似行列と二次形式"
tags:
  - institute-of-science-tokyo
  - Mathematics.Linear-Algebra.Eigenvalues-and-Eigenvectors
  - Mathematics.Linear-Algebra.Quadratic-Form
  - Mathematics.Linear-Algebra.Orthogonal-Matrix
  - Mathematics.Linear-Algebra.Inner-Product-and-Orthogonality
---
# 東京工業大学 工学院 情報通信系 2018年8月実施 H2 相似行列と二次形式

## **Author**
[思齐塾](https://www.siqishu.com/), 祭音Myyura (co-authored with GPT 5.6 SOL)&#x20;

## **Description**

H2.

1） 2 つの正方行列 $A, B$ に対して，

$$
B = P^{-1}AP \tag{H2.1}
$$

が成り立つ正則な行列 $P$ が存在するとき，行列 $A, B$ は相似であるとよぶ．このとき，以下の問に答えよ．

   a) 行列 $A, B$ が相似であるとき，これらが同じ固有値を持つことを示せ．
   b) 式 (H2.1) において $A = \begin{pmatrix} 4 & -3 \\ 2 & -1 \end{pmatrix}$ , $P = \begin{pmatrix} 2 & 5 \\ 1 & 3 \end{pmatrix}$ であるとき，行列 $A, B$ のそれぞれについて，すべての固有値とこれらに対応する長さ 1 の固有ベクトルを求めよ．

2） 実変数 $x, y, z$ の二次形式 $f(x, y, z) = 2x^2 + 2y^2 + 4z^2 + 2xy + 2yz + 2zx$ が与えられるとき，以下の問に答えよ．

   a) $f(x, y, z)$ を，実対称行列 $T$ を用いて $\begin{pmatrix} x & y & z \end{pmatrix} T \begin{pmatrix} x \\ y \\ z \end{pmatrix}$ と表すとき， $T$ を求めよ．
   b) $f(x, y, z)$ は，直交行列 $Q$ を用いた変換 $\begin{pmatrix} x \\ y \\ z \end{pmatrix} = Q \begin{pmatrix} s \\ t \\ u \end{pmatrix}$ を用いて，
      標準形 $a_1s^2 + a_2t^2 + a_3u^2$ に変換できる．ただし， $a_1, a_2, a_3$ は実数で， $a_1 \le a_2 \le a_3$ とする．このとき， $Q$ 及び， $a_1, a_2, a_3$ を求めよ．
   c) 二次曲面 $f(x, y, z) - 2\sqrt{3}x - 2\sqrt{3}y + 2\sqrt{3}z - 8 = 0$ が楕円面であることを示せ．

### 题目描述

1. 对两个方阵 $A,B$，若存在可逆矩阵 $P$ 使

$$
B=P^{-1}AP,
\tag{H2.1}
$$

则称 $A,B$ 相似。

   1. 证明相似矩阵 $A,B$ 具有相同的特征值。
   2. 在 (H2.1) 中取

   $$
   A=\begin{pmatrix}4&-3\\2&-1\end{pmatrix},
   \qquad
   P=\begin{pmatrix}2&5\\1&3\end{pmatrix}.
   $$

   分别求 $A$ 与 $B$ 的全部特征值，并对每个特征值求一个对应的长度为 $1$ 的特征向量。

2. 给定实变量二次形式

$$
f(x,y,z)
=2x^2+2y^2+4z^2+2xy+2yz+2zx.
$$

   1. 求实对称矩阵 $T$，使

   $$
   f(x,y,z)
   =
   \begin{pmatrix}x&y&z\end{pmatrix}
   T
   \begin{pmatrix}x\\y\\z\end{pmatrix}.
   $$

   2. 通过正交变换

   $$
   \begin{pmatrix}x\\y\\z\end{pmatrix}
   =
   Q\begin{pmatrix}s\\t\\u\end{pmatrix}
   $$

   将 $f$ 化为标准形

   $$
   a_1s^2+a_2t^2+a_3u^2,
   \qquad a_1\leq a_2\leq a_3.
   $$

   求正交矩阵 $Q$ 以及 $a_1,a_2,a_3$。
   3. 证明二次曲面

   $$
   f(x,y,z)
   -2\sqrt3x-2\sqrt3y+2\sqrt3z-8=0
   $$

   是椭球面。

## **Kai**

### 1)a)

$$
\det(\lambda I-B)=\det\!\bigl(P^{-1}(\lambda I-A)P\bigr)=\det(\lambda I-A).
$$

特性多項式が等しいので，相似な行列の固有値は重複度を含めて一致する。

### 1)b)

$$
\det(\lambda I-A)=(\lambda-1)(\lambda-2),\quad
P^{-1}=\begin{pmatrix}3&-5\\-1&2\end{pmatrix},\quad
B=\begin{pmatrix}0&-2\\1&3\end{pmatrix}.
$$

単位固有ベクトルの一例は次のとおり（各ベクトルの符号反転も可）。

| 固有値 | $A$ | $B$ |
|---|---|---|
| $1$ | $(1,1)^{\mathsf T}/\sqrt2$ | $(-2,1)^{\mathsf T}/\sqrt5$ |
| $2$ | $(3,2)^{\mathsf T}/\sqrt{13}$ | $(-1,1)^{\mathsf T}/\sqrt2$ |

### 2)a),b)

$$
T=\begin{pmatrix}2&1&1\\1&2&1\\1&1&4\end{pmatrix},\qquad
\det(\lambda I-T)=(\lambda-1)(\lambda-2)(\lambda-5).
$$

対応する固有ベクトルを正規化して並べると

$$
Q=\begin{pmatrix}
1/\sqrt2&1/\sqrt3&1/\sqrt6\\
-1/\sqrt2&1/\sqrt3&1/\sqrt6\\
0&-1/\sqrt3&2/\sqrt6
\end{pmatrix},\quad Q^{\mathsf T}Q=I,\quad Q^{\mathsf T}TQ=\operatorname{diag}(1,2,5).
$$

したがって $\boxed{(a_1,a_2,a_3)=(1,2,5)}$。

### 2)c)

$(x,y,z)^{\mathsf T}=Q(s,t,u)^{\mathsf T}$ を代入すると

$$
s^2+2t^2+5u^2-6t-8=0,
$$

すなわち

$$
\boxed{\frac{s^2}{25/2}+\frac{(t-3/2)^2}{25/4}+\frac{u^2}{5/2}=1}.
$$

分母がすべて正なので楕円面である。
