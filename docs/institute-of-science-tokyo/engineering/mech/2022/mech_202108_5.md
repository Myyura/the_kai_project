---
sidebar_label: "2021年8月実施 選択専門科目 問題5（工業数学）"
tags:
  - institute-of-science-tokyo
  - Mathematics.Geometry.Torus-Volume
  - Mathematics.Differential-Equations.First-Order-Ordinary-Differential-Equation
  - Mathematics.Linear-Algebra.Matrix-Diagonalization
  - Mathematics.Linear-Algebra.Matrix-Power
  - Mathematics.Complex-Analysis.Residue-at-Higher-Order-Pole
  - Mathematics.Fourier-Analysis.Fourier-Series
  - Mathematics.Fourier-Analysis.Parseval-Identity
---
# 東京工業大学 工学院 機械系 2021年8月実施 選択専門科目 問題5（工業数学）


## **Author**
[Miyake](https://miyake.github.io/exams/index.html), 祭音Myyura

## **Description**

[公式問題・12〜13ページ](https://admissions.isct.ac.jp/plugins/cms/component_download_file.php?contentsDataId=&contentsId=&fileName=exam_mech_20218&key=fd8e1a22d9974f5a988fdbd9379b05c2.pdf&pageId=3186&prevId=&type=1)


### 問1
1. $b>2a>0$ とする。円板 $x^2+(y-b)^2\le a^2$ を直線 $y=x$ のまわりに回転した立体の体積を求める。
2. $dy/dx=(x+y)^2$ の一般解を求める。代換 $u=x+y$ を用いてよい。

### 問2
$A=\begin{pmatrix}2&1\\1&2\end{pmatrix}$ について、(1) 固有値と正規化した固有ベクトル、(2) 自然数 $n$ に対する $A^n$ を求める。

### 問3
1. 複素関数 $1/(1+z)$ の Maclaurin 級数を求める。
2. $1/[z(e^z-1)]$ の $z=0$ における留数を求める。

### 問4
周期 $T$ の実関数の複素 Fourier 展開を $f(x)=\sum_{n=-\infty}^{\infty}c_ne^{2\pi inx/T}$ とする。不連続点は考慮しなくてよい。

1. 周期 $T=2$ の矩形波を
   $$
   p(x)=\begin{cases}
   0,&-1<x\le-\frac12,\\
   1,&-\frac12<x\le\frac12,\\
   0,&\frac12<x\le1
   \end{cases}
   $$
   と定め、周期的に延長する。$c_0,c_1,c_2,c_3$ の実部・虚部をそれぞれ空欄①〜⑧に記す。
2. $c_{-n}=c_n^*$ を用い、$q(x)=\sum_{n=-3}^{3}c_ne^{in\pi x}$ に対する $\int_0^2q(x)^2\,dx$ を空欄⑨に記す。

![周期2の矩形波](https://raw.githubusercontent.com/Myyura/the_kai_project_assets/main/kakomonn/institute-of-science-tokyo/engineering/mech/2022/science-tokyo-mech-202108-pulse.svg)

### 题目描述

#### 问1

1. 设 $b>2a>0$，求圆盘 $x^2+(y-b)^2\le a^2$ 绕直线 $y=x$ 旋转所得立体的体积。圆盘半径为 $a$，圆心到旋转轴的距离为 $b/\sqrt2$。
2. 求 $dy/dx=(x+y)^2$ 的通解，可以使用代换 $u=x+y$。由 $u'=1+u^2$ 得到 $u=\tan(x+C)$，再回代为 $y=\tan(x+C)-x$。

#### 问2

给定 $A=\begin{pmatrix}2&1\\1&2\end{pmatrix}$。
1. 求特征值及各自的归一化特征向量。
2. 用正交对角化求自然数 $n$ 对应的 $A^n$。

#### 问3

1. 求 $1/(1+z)$ 的 Maclaurin 级数（在 $|z|<1$ 内）。
2. 求 $1/[z(e^z-1)]$ 在 $z=0$ 处的留数。

#### 问4

周期 $T$ 的实函数的复 Fourier 展开约定为 $f(x)=\sum_{n\in\mathbb Z}c_ne^{2\pi inx/T}$；不用考虑不连续点的取值。

1. 周期 $T=2$ 的矩形波在一个周期上为
   $$
   p(x)=\begin{cases}0,&-1<x\le-1/2,\\1,&-1/2<x\le1/2,\\0,&1/2<x\le1.\end{cases}
   $$
   求 $c_0,c_1,c_2,c_3$ 的实部和虚部（空栏①–⑧）。系数按 $c_n=\frac12\int_{-1}^{1}p(x)e^{-in\pi x}\,dx$ 归一化。
2. 利用 $c_{-n}=c_n^*$，把 $q(x)=\sum_{n=-3}^{3}c_ne^{in\pi x}$ 写为余弦形式，并求 $\int_0^2q(x)^2\,dx$（空栏⑨）。

## **Kai**
### 問1
#### (1)
<a href="https://manabitimes.jp/math/2646">ドーナツ（トーラス体・円環体）の体積・表面積を2通りの方法で計算</a>
の定理1を用いる。円板の中心 $(0,b)$ から回転軸 $y=x$ までの距離は $R=b/\sqrt2$、円板の半径は $a$ である。$b>2a$ より軸は円板と交わらないため、求める体積は、

$$
\begin{aligned}
2\pi^2a^2\frac{b}{\sqrt2}
=\sqrt2\,\pi^2a^2b
\end{aligned}
$$

である。

#### (2)
$u=x+y$ とおくと、

$$
  \begin{aligned}
  \frac{du}{dx}
  &= 1 + \frac{dy}{dx}
  \\
  &= 1 + u^2
  \end{aligned}
$$

となり、

$$
  \begin{aligned}
  \frac{du}{1+u^2} &= dx
  \\
  \arctan u &= x + C
  \\
  u &= \tan (x+C)
  \end{aligned}
$$

したがって、

$$
  \begin{aligned}
  y &= \tan (x+C)-x
  \end{aligned}
$$

を得る。
ここで $C$ は積分定数である。

### 問2
#### (1)
$A$ の固有値を $\lambda$ とすると、

$$
\begin{aligned}
0
&= \det \begin{pmatrix} 2 - \lambda & 1 \\ 1 & 2 - \lambda \end{pmatrix}
= (\lambda - 1)(\lambda - 3)
\\
\therefore \ \ 
\lambda &= 1, 3
\end{aligned}
$$

である。
固有値 $1$ に属する固有ベクトルを求めるために、

$$
\begin{aligned}
\begin{pmatrix} 1 & 1 \\ 1 & 1 \end{pmatrix}
\begin{pmatrix} x \\ y \end{pmatrix}
=
\begin{pmatrix} 0 \\ 0 \end{pmatrix}
\end{aligned}
$$

とおくと、 $x+y=0$ なので、正規化された固有ベクトルは、例えば、

$$
\begin{aligned}
\frac{1}{\sqrt{2}}
\begin{pmatrix} 1 \\ -1 \end{pmatrix}
\end{aligned}
$$

である。
固有値 $3$ に属する固有ベクトルを求めるために、

$$
\begin{aligned}
\begin{pmatrix} -1 & 1 \\ 1 & -1 \end{pmatrix}
\begin{pmatrix} x \\ y \end{pmatrix}
=
\begin{pmatrix} 0 \\ 0 \end{pmatrix}
\end{aligned}
$$

とおくと、 $x=y$ なので、正規化された固有ベクトルは、例えば、

$$
\begin{aligned}
\frac{1}{\sqrt{2}}
\begin{pmatrix} 1 \\ 1 \end{pmatrix}
\end{aligned}
$$

である。

#### (2)
(1)で求めた固有ベクトルを使って、

$$
\begin{aligned}
P = \frac{1}{\sqrt{2}} \begin{pmatrix} 1 & 1 \\ -1 & 1 \end{pmatrix}
\end{aligned}
$$

とすると、

$$
\begin{aligned}
P^{-1} &= \frac{1}{\sqrt{2}} \begin{pmatrix} 1 & -1 \\ 1 & 1 \end{pmatrix}
\\
P^{-1} AP &= \begin{pmatrix} 1 & 0 \\ 0 & 3 \end{pmatrix}
\end{aligned}
$$

なので、

$$
\begin{aligned}
A^n
&= P \begin{pmatrix} 1 & 0 \\ 0 & 3^n \end{pmatrix} P^{-1}
\\
&= \frac{1}{2} \begin{pmatrix} 1+3^n & -1+3^n \\ -1+3^n & 1+3^n \end{pmatrix}
\end{aligned}
$$

を得る。

### 問3
#### (1)

$$
  \begin{aligned}
  \frac{1}{1+z}
  &= \frac{1}{1-(-z)}
  \\
  &= \sum_{n=0}^\infty (-z)^n
  \\
  &= 1 -z + z^2 - z^3 + z^4 - \cdots
  \end{aligned}
$$

#### (2)
$e^z$ のマクローリン展開が $e^z = 1 + z + (1/2)z^2 + \cdots$ であることを考慮すると、
与えられた関数は $z=0$ で2位の極をもつことがわかり、その留数は次のように計算できる：

$$
  \begin{aligned}
  \lim_{z \to 0} \frac{d}{dz} z^2 \cdot \frac{1}{z \left( e^z - 1 \right) }
  &= \lim_{z \to 0} \frac{d}{dz} \frac{z}{e^z - 1}
  \\
  &= \lim_{z \to 0} \frac{\left( e^z - 1 \right) - z e^z}{\left( e^z - 1 \right)^2}
  \\
  &= \lim_{z \to 0}
  \frac{\left( z + \frac{1}{2} z^2 + \cdots \right) - z \left( 1 + z + \cdots \right)}
  {\left( z + \frac{1}{2} z^2 + \cdots \right)^2}
  \\
  &= \lim_{z \to 0} \frac{- \frac{1}{2} z^2 + \cdots}{z^2 + \cdots}
  \\
  &= - \frac{1}{2}
  \end{aligned}
$$

### 問4
#### (1)
周期 $T=2$ の複素 Fourier 係数には $1/T$ の係数が必要である。

$$
c_n=\frac12\int_{-1}^{1}p(x)e^{-in\pi x}\,dx
=\begin{cases}
\frac12,&n=0,\\
\dfrac{\sin(n\pi/2)}{n\pi},&n\ne0.
\end{cases}
$$

従って

$$
c_0=\frac12,\qquad c_1=\frac1\pi,\qquad c_2=0,\qquad c_3=-\frac1{3\pi}.
$$

全て実数であり、空欄①〜⑧は $\frac12,0,\frac1\pi,0,0,0,-\frac1{3\pi},0$ となる。

#### (2)
共役対称性より

$$
q(x)=\frac12+\frac2\pi\cos(\pi x)-\frac2{3\pi}\cos(3\pi x).
$$

異なる周波数の直交性を用いると、

$$
\int_0^2q(x)^2\,dx
=2\left(\frac12\right)^2+\left(\frac2\pi\right)^2+\left(\frac2{3\pi}\right)^2
=\boxed{\frac12+\frac{40}{9\pi^2}}.
$$

これが空欄⑨である。
