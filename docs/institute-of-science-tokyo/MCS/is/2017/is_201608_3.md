---
sidebar_label: "2016年8月実施 午前 問3"
tags:
  - institute-of-science-tokyo
  - Mathematics.Functional-Analysis.Normed-Space
  - Mathematics.Functional-Analysis.Uniform-Convergence
---
# 東京工業大学 情報理工学院 数理・計算科学系 2016年8月実施 午前 問3


## **Author**

祭音Myyura (co-authored with GPT 5.6 SOL)&#x20;

## **Description**

区間 $[0,1]$ 上の連続関数全体のなす線形空間 $C[0,1]$ に対し、

$$
\|u\|=\max_{0\leq x\leq1}|u(x)|
$$

とおく。

(1) $\|\cdot\|$ が $C[0,1]$ のノルムとなることを示せ。以下、このノルムを導入した空間を $X$ と表す。

(2) 多項式全体の集合 $P$ が $X$ の線形部分空間であることを示せ。

(3) 自然数 $n$ に対して

$$
u_n(x)=1+\frac{x}{2}+\left(\frac{x}{2}\right)^2+\cdots+
\left(\frac{x}{2}\right)^{n-1}
$$

と定義する。$n\to\infty$ のとき、$u_n$ は $X$ で収束することを示せ。

(4) $n\to\infty$ のとき、$u_n$ は $P$ で収束しないことを示せ。

### 题目描述

设 $C[0,1]$ 为区间 $[0,1]$ 上全体连续函数组成的向量空间，并定义

$$
\|u\|=\max_{0\leq x\leq1}|u(x)|.
$$

1. 证明 $\|\cdot\|$ 是 $C[0,1]$ 上的范数。此后记配备该范数的空间为 $X$。
2. 设 $P$ 为全体多项式组成的集合，证明 $P$ 是 $X$ 的线性子空间。
3. 对每个自然数 $n$，令

   $$
   u_n(x)=1+\frac{x}{2}+\left(\frac{x}{2}\right)^2+\cdots+
   \left(\frac{x}{2}\right)^{n-1}.
   $$

   证明当 $n\to\infty$ 时，$u_n$ 在 $X$ 中收敛。
4. 证明当 $n\to\infty$ 时，$u_n$ 在子空间 $P$ 中不收敛。

## **Kai**
### (1)

連続性と区間 $[0,1]$ のコンパクト性により最大値は存在する。任意の $u,v\in C[0,1]$ とスカラー $\alpha$ について

$$
\|u\|\geq0,
\qquad
\|u\|=0\Longleftrightarrow u=0,
$$

$$
\|\alpha u\|=|\alpha|\|u\|,
\qquad
\|u+v\|\leq\|u\|+\|v\|
$$

が成り立つ。最後の不等式は各 $x$ での三角不等式を最大化すれば得られる。よって $\|\cdot\|$ はノルムである。

### (2)

零多項式は $P$ に属し、多項式 $p,q$ とスカラー $\alpha,\beta$ に対して $\alpha p+\beta q$ も多項式である。また多項式は $[0,1]$ 上連続なので $P\subset X$ である。したがって $P$ は $X$ の線形部分空間である。

### (3)

等比級数より

$$
u_n(x)=\frac{1-(x/2)^n}{1-x/2}
$$

である。$u(x)=1/(1-x/2)$ とおくと

$$
\begin{aligned}
\|u_n-u\|
&=\max_{0\leq x\leq1}\frac{(x/2)^n}{1-x/2}\\
&=\frac{(1/2)^n}{1-1/2}=2^{1-n}\longrightarrow0.
\end{aligned}
$$

よって $u_n$ は $X$ において $u$ に収束する。

### (4)

$P$ が $X$ から受け継ぐ同じノルムで $u_n$ がある $p\in P$ に収束すると仮定する。$X$ における極限の一意性により

$$
p(x)=\frac1{1-x/2}
$$

でなければならない。しかし多項式 $p$ がこの恒等式を満たすなら $(1-x/2)p(x)=1$ となる。非零多項式の次数を比較すると左辺は $\deg p+1$、右辺は 0 であり矛盾する。ゆえに $u_n$ は $P$ では収束しない。
